# Commands I used

## Terraform
terraform init
terraform apply
terraform output

## Test Lambda
aws lambda invoke --function-name <lambda_name> out.json

## Check S3 report
aws s3 ls s3://<bucket>/reports/
